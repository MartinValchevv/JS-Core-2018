function endWith(text, end) {
    console.log(text.endsWith(end));
}

endWith('This sentence ends with fun?', 'fun?')
endWith('This is Houston, we have…', 'We have…')
endWith('The new iPhone has no headphones jack.', 'o headphones jack.')